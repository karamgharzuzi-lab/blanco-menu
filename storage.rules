rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Dish photos. Anyone can VIEW them. Only authenticated admins can upload/replace.
    match /dishes/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
